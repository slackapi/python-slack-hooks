# pip install -r requirements.txt
# Note: comments here must be full-line only; this file is read into the
# package's dependencies via [tool.setuptools.dynamic] in pyproject.toml.

# slack_bolt
slack_bolt>=1.29.0,<2

# packaging
packaging<27
